# OS / editor
.DS_Store
Thumbs.db
desktop.ini
.vscode/
.idea/
*.swp
*.swo
*~

# Claude Code local state (per-machine, not for the repo)
.claude/

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Env / secrets (defense in depth — config uses placeholders today,
# but keep this so a future real key is never committed by accident)
.env
.env.*
!.env.example

# Node / build artifacts
node_modules/
dist/
build/
.cache/
